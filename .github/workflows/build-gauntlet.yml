name: Build Gauntlet Leg 3

on:
  # ✅ Manual / API-triggered (your button)
  workflow_dispatch: {}

  # ✅ Scheduled runs – script itself will skip outside game windows
  schedule:
    - cron: "* * * * *"  # every 15 minutes

jobs:
  build-gauntlet:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build Gauntlet Leg 3 payload
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          # 👇 Manual runs (workflow_dispatch) set GAUNTLET_FORCE=1, scheduled runs keep it empty
          GAUNTLET_FORCE: ${{ github.event_name == 'workflow_dispatch' && '1' || '' }}
        run: |
          node script/buildgauntlet.mjs
